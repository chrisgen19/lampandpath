; PHP upload limits for the production image (Dockerfile). PHP's defaults
; (2 MB per file, 8 MB per request) reject photos straight from a phone. The
; request limit is higher, since a request carries more than the file.
upload_max_filesize = 64M
post_max_size = 72M
