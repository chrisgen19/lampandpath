# Browser caching for static files, served by the production image (Dockerfile).
#
# Theme CSS and JS URLs carry a version query that changes with every build, so
# they can be cached for a year. Font file names are fixed: give a font a new
# file name if it is ever replaced. Media keeps a month.
<IfModule mod_expires.c>
	ExpiresActive On
	ExpiresByType text/css "access plus 1 year"
	ExpiresByType text/javascript "access plus 1 year"
	ExpiresByType application/javascript "access plus 1 year"
	ExpiresByType font/woff2 "access plus 1 year"
	ExpiresByType image/png "access plus 1 month"
	ExpiresByType image/jpeg "access plus 1 month"
	ExpiresByType image/webp "access plus 1 month"
	ExpiresByType image/gif "access plus 1 month"
	ExpiresByType image/svg+xml "access plus 1 month"
</IfModule>
